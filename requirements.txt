# travian-local — dépendances directes (installer dans le venv à la racine)
#   TMPDIR=venv/.piptmp ./venv/bin/pip install -r requirements.txt
#
# Serveur / API / UI
fastapi==0.138.1
uvicorn[standard]==0.49.0
sqlmodel==0.0.39
jinja2==3.1.6
httpx==0.28.1
pillow==12.2.0
python-dotenv==1.2.2
pyyaml==6.0.3
#
# Phase 4 — macros pilotées par Claude Code (agent LLM local, pas d'API Anthropic).
# Requiert le CLI `claude` (Claude Code) installé et authentifié.
claude-agent-sdk==0.2.110
#
# Tests
pytest==9.1.1
